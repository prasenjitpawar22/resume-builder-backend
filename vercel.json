{
    "version": 2,
    "buildCommand": "rimraf ./build && tsc && npm run build",
    "builds": [
        {
            "src": "build/index.js",
            "use": "@vercel/node" 
            
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}