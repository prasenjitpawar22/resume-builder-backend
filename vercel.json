{
    "version": 2,
    "buildCommand": "rimraf ./build && tsc && npm run build",
    "builds": [
        {
            "src": "index.ts",
            "use": "@vercel/node" 
            
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}